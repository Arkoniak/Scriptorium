[project]
name = "scriptorium"
version = "0.1.0"
description = "Research pipeline comparing OCR/VLM approaches for converting scanned PDF books into clean EPUBs"
requires-python = ">=3.14"
dependencies = []
