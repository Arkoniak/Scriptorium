<!--
Copy this file to docs/experiments/YYYY-MM-DD-short-slug.md and fill it in.
Add a row to docs/experiments/README.md pointing here once the entry is written.
-->

# <Question being tested>

## Setup
- Model(s): <name, version, quantization>
- Input: <which book/pages, what type — clean text, table, footnotes, poor scan, ...>
- Params/prompt: <anything non-default>

## Result
<What happened, in prose. Link to raw artifacts by path rather than pasting them inline —
e.g. `books/output/<...>` — and note if those artifacts may no longer be reproducible.>

## Conclusion
<What this changes or confirms. Link back to the relevant section of docs/brainstorm.md —
a Stage, a Key Tension, or a Rejected Idea — that this result informs.>

Rationale: <known | unknown — note explicitly if part of the "why" couldn't be determined>
